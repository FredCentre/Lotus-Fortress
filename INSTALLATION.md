# Windows

Bấm chuột phải vào file `windows_install.ps1` và chọn `Run with PowerShell`, làm theo hướng dẫn trong bộ cài đặt

# Linux
> Lưu ý: Phương pháp này sẽ không hoạt động với TF2 được chạy qua Proton, vui lòng sử dụng bản native.

Bước 1: Mở Terminal trong thư mục Lotus Fortress

Bước 2: Chạy lệnh sau:

```
chmod +x linux_install.sh
./linux_install.sh
```

Bước 3: Làm theo hướng dẫn trong bộ cài đặt

# Tự cài thủ công
> Nếu bạn không tin tưởng bộ cài đặt của chúng tôi hoặc bộ cài đặt không hoạt động trong một số trường hợp, bạn hoàn toàn có thể tự cài thủ công qua các bước sau

Bước 1: Giải nén Lotus Fortress sau khi tải về [tại đây.](https://github.com/FredCentre/Lotus-Fortress/blob/main/INSTALLATION.md)

Bước 2: Trên Steam, bạn bấm chuột phải vào Team Fortress 2 trong thư viện > `Manage` > `Browse local files`. Bước này sẽ mở thư mục chứa dữ liệu của game

Bước 3: Trong Lotus Fortress sẽ có 2 thư mục, đó là `tf` và `hl2`, bạn chỉ cần mang 2 thư mục trên vào thư mục chứa dữ liệu của game, khi hệ thống hỏi, bạn chỉ cần bấm chọn thay thế là xong
